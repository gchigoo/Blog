<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>后台管理</title>
  <link rel="stylesheet" href="<%= typeof assetUrl === 'function' ? assetUrl('/vendor/inter.css') : '/vendor/inter.css' %>">
  <link rel="stylesheet" href="<%= typeof assetUrl === 'function' ? assetUrl('/vendor/new.css/new.min.css') : '/vendor/new.css/new.min.css' %>">
  <link rel="stylesheet" href="<%= typeof assetUrl === 'function' ? assetUrl('/css/custom.css') : '/css/custom.css' %>">
</head>
<body>
  <header>
    <h1>后台管理</h1>
    <nav>
      <a href="/">首页</a> /
      <a href="/about">关于</a> /
      <a href="/admin/upload">上传文章</a> /
      <a href="/admin/articles">文章列表</a> /
      <a href="/admin/analytics">访问统计</a> /
      <% if (typeof commentsEnabled !== 'undefined' && commentsEnabled) { %>
        <a href="/admin/comments">评论审核</a> /
      <% } %>
      <a href="#" id="logoutBtn">登出</a>
    </nav>
  </header>
  
  <main>
